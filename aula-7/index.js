const nome = 'Gabriel';

console.log(nome);

 const primeiroNumero = 5; // Number
 const segundoNumero = 10;

 const resultado = primeiroNumero * segundoNumero
 const resultadoDuplicado = resultado * 2;
 let resultadoTriplicado = resultado * 3;
 resultadoTriplicado = resultadoTriplicado + 5;

 console.log(resultadoTriplicado);

 console.log(typeof resultadoTriplicado);
 
 // Operadores Aritmético +, -, *, /
 

// Não podemos criar constantes com palavras reservadas

// Contantes precisam ter nomes significativos

// Não pode começar um nome de uma contante com número

// Constantes não podem conter espaços ou traços

// Utilizamos camelCase

// Case-sensetive, a letra maiúscula e minúscula fazem diferença

// Não pode mondificar o valor de uma constante

// NÃO UTILIZE "VAR", UTILIZE "CONST"!!

